function whichMatrixCreate(which) {
    if (which == "A") {
        createMathMatrix(which, matrixA, rowA, columnA);
    }
    if (which == "B") {
        createMathMatrix(which, matrixB, rowB, columnB);
    }
}